on:
  push:
    branches:
      - master


      jobs:
        build: 
          runs_on: ubuntu-latest  

          steps:
            - name: 'testing github actions' 
              run: pwd

            - name: 'testing printing' 
              run: echo Amged
                
